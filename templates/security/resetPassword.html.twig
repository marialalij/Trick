{% extends 'dashboard.html.twig' %}

{% block title %}Profile
{% endblock %}

{% block subtitle %}
	Reset password
{% endblock %}

{% block body %}
	<div class="py-4 px-4 dashboard-div" id="resetPass">
		{% for message in app.flashes('success') %}
			<div class="alert alert-info w-100 mt-1 text-center">
				{{ message }}
			</div>
		{% endfor %}
		{% for message in app.flashes('danger') %}
			<div class="alert alert-danger w-100 mt-1 text-center">
				{{ message }}
			</div>
		{% endfor %}
		{{ form_start(form) }}
		<div class="row pt-2">
			<div class="col-md-6 mx-auto">
				{{ form_row(form.oldPassword) }}
				{{ form_row(form.plainPassword) }}
			</div>
		</div>
		<div class="row justify-content-center">
			<button type="submit" class="btn btn-info">Save changes</button>
		</div>
		{{ form_end(form) }}

		<a class="custom-link text-center mt-3" href="{{ path('app_forgotten_password') }}">Forgot password ?</a>
	</div>

{% endblock %}
